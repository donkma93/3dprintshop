name: admin_app
description: 3D Print Shop — Admin mobile (Android + iOS). BASE URL cấu hình lúc đăng nhập.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.3
  mobile_scanner: ^6.0.2
  intl: ^0.19.0
  device_info_plus: ^11.1.1
  cached_network_image: ^3.4.1
  share_plus: ^10.1.2
  url_launcher: ^6.3.1
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
